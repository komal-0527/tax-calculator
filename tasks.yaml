---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: build
spec:
  workspaces:
    - name: source
  steps:
    - name: docker-build
      image: docker:24
      workingDir: $(workspaces.source.path)
      script: |
        #!/bin/sh
        docker build -t tax-calculator .